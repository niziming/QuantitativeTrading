server:
  port: 8701
spring:
  profiles:
    active: common, plus
  application:
    name: service-app

logging:
  level:
    cn.zm: debug

global:
  config:
    host: http://localhost:8000
    services:
      stock_zh_a_new: stock_zh_a_new  # 股票数据次新股
      macro_china_xfzxx: macro_china_xfzxx  # 消费者信心指数

# eureka
#eureka:
#  client:
#    serviceUrl:
#      defaultZone: http://localhost:8700/eureka/
